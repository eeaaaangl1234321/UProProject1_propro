using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RunMove : BaseMove
{
    public RunMove(float speed, Transform moveTransform, Animator anim, string nameClip) : base(speed, moveTransform, anim, nameClip)
    {
    }
}
